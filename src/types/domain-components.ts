/**
 * The different components of a websites domain name, usually separated by the "." character
 */
export interface IDomainComponents {
  /**
   * t.b.d
   */
  subdomain: string;
  /**
   * t.b.d
   */
  parentDomain: string;
}
